Use whatever HTML, CSS, JS, frameworks, or CDN libraries fit the feature; no need to ask before adding one.
Keep index.html at the repo root, and use relative paths (style.css, never /style.css).
Split code into separate files: style.css for styles, script.js for behavior, nothing inline.
Commit as you go, but tell me each time you push to GitHub.
